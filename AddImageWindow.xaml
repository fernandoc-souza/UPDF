<Window x:Class="PdfToolbox.AddImageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Imagem" Height="150" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Caminho da Imagem:" FontWeight="SemiBold" Grid.Row="0"/>
        <Grid Grid.Row="1" Margin="0,5,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtImagePath" Grid.Column="0" IsReadOnly="True" Margin="0,0,5,0"/>
            <Button Content="Procurar..." Grid.Column="1" Width="80" Click="BtnBrowse_Click"/>
        </Grid>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="80" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="Próximo (Posicionar...)" Width="150" Background="#0078D7" Foreground="White" Click="BtnApply_Click"/>
        </StackPanel>
    </Grid>
</Window>
